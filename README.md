# FractionalAI

Static site for fractionalai.net — deployed via Cloudflare Pages.
Single-file site: edit `index.html`, push to main, Pages auto-deploys.
Branch pushes get automatic preview URLs (*.pages.dev).
